# Random Dog Picture Extension

Simple extension that fetches (heh) a picture of a dog from the [Dog API](https://dog.ceo/dog-api/) and displays it in an extension popup whenever the extension icon is clicked.